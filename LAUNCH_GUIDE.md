# دليل إطلاق MY Academy كمنصة SaaS متعددة الأكاديميات

**الإصدار:** 13 أغسطس 2026  
**المالك التقني:** مالك مشروع MY Academy  
**حالة الإطلاق:** جاهز وظيفياً وتقنياً؛ يبقى تفعيل الأسرار التجارية والنطاق وإلغاء PAT المكشوف قبل الإطلاق التجاري العام

> **حالة التحقق الحالية:** تم نشر commit `b34ace8` على Vercel Production بنجاح. أُصلح مسار مطابقة المدرس عند فراغ اللقطة، وأصبحت لوحة المدرس الحية تعرض البيانات دون رسالة `No teacher profile linked`، كما عولجت قراءات الاسم والملخصات الناقصة في لوحة ولي الأمر. اختُبرت حسابات العرض الأربعة محليًا عبر 58 اختبار Playwright، وتحققنا حيًا من `/teacher` و`/parent`. قبل الإطلاق التجاري، يبقى إدخال مفاتيح المالك التجارية (Resend وPaymob/Stripe)، ربط النطاق والتحقق من DNS، وإلغاء PAT المكشوف. لا تستخدم حسابات العرض مع عملاء حقيقيين.

## 1. ما الذي أصبح جاهزاً

تحولت المنصة من تطبيق أكاديمية واحدة إلى أساس SaaS متعدد الأكاديميات. أصبح مصدر التفويض هو عضوية صريحة ونشطة في الأكاديمية، وليس مجرد الدور المخزن في الملف الشخصي. وتُوقّع جلسة التطبيق باستخدام HMAC، وتُحمّل لقطة البيانات حسب `academy_id` المعتمد في الجلسة، بينما تحمي سياسات RLS في Supabase طبقة قاعدة البيانات. كما تُنشأ الحسابات الجديدة عبر دعوات ذات رمز لمرة واحدة محفوظ على شكل تجزئة فقط.

| المجال | الحالة | الضمان العملي |
|---|---:|---|
| عزل الأكاديميات | مكتمل | لقطات مخزن مستقلة لكل أكاديمية، إجراءات خادمية مقيدة بالجلسة، وRLS في PostgreSQL. |
| الأدوار | مكتمل | أدوار `ADMIN` و`TEACHER` و`PARENT` و`STUDENT` تعمل عبر سياسة مركزية وجلسة موقعة. |
| الدعوات | مكتمل | يصدر المدير رابطاً محدود الصلاحية؛ لا تُرسل كلمة مرور بالبريد ولا يوجد تسجيل ذاتي بكود عام. |
| الفوترة | مكتمل برمجياً | Stripe وPaymob يفعّلان الخطة من ويب هوك موثق فقط، لا من المتصفح. |
| الاختبارات | مكتمل محلياً | نجحت 58 حالة Playwright في رحلات المدير والمدرس وولي الأمر والطالب والأمان والفوترة، إضافة إلى نجاح TypeScript وNext production build. |
| نشر الإنتاج | مكتمل للإصدار الحالي | نشر Vercel المرتبط بـ commit `b34ace8c4d20b9fdfa9dd661477e6b835588bccb` حالته `READY` على Production. يجب إعادة التحقق من المتغيرات عند تدوير الأسرار أو تغيير النطاق. |

> **مبدأ الإطلاق:** لا تعتبر عملية الدفع ناجحة في MY Academy لمجرد عودة المستخدم من صفحة الدفع. لا يُفعّل الاشتراك إلا بعد وصول إشعار ويب هوك صحيح التوقيع وتسجيله بصورة idempotent.

## 2. الإجراء الإلزامي الأول: إصلاح اتصال Vercel بـ Supabase

افتح مشروع **my-academy-eg** في Vercel ثم انتقل إلى **Settings → Environment Variables**. أضف أو استبدل القيم التالية في بيئة **Production** بالقيم من مشروع Supabase المسمى **mySAAS** ذي المعرّف `fpcdaiyktnoiwaulbhcn`. لا تضع مفاتيح خدمة أو أسرار في المستودع أو في أي متغير يبدأ بـ`NEXT_PUBLIC_`.

| المتغير | مصدر القيمة | النطاق | ملاحظات إلزامية |
|---|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase → Settings → API | Production | عنوان مشروع `mySAAS` فقط. |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase → Settings → API | Production | المفتاح العام/anon للمشروع نفسه فقط. |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase → Settings → API | Production فقط | سر خادمي؛ لا يُعرض ولا يبدأ بـ`NEXT_PUBLIC_`. |
| `SESSION_SECRET` | أنشئه بأمر `openssl rand -base64 48` | Production | قيمة عشوائية فريدة بطول 32 حرفاً على الأقل. |
| `NEXT_PUBLIC_APP_URL` | عنوان المنصة النهائي HTTPS | Production | مثال: `https://my-academy-eg.vercel.app` أو النطاق المخصص. |

بعد الحفظ، أعد نشر آخر نسخة من فرع `main`. تغييرات متغيرات Vercel لا تسري على نشر قائم؛ يجب إنشاء نشر جديد كي تُطبّق القيم.[1]

### اختبار القبول بعد إعادة النشر

نفّذ الاختبارات التالية في نافذة متخفية أو متصفح لا يحمل جلسة قديمة. يجب أن يؤدي كل حساب إلى البوابة الصحيحة، ولا ينبغي مشاركة الحسابات خارج بيئة العرض.

| الدور | البريد | كلمة المرور | النتيجة المتوقعة |
|---|---|---|---|
| مدير العرض | `admin@myacademy.edu` | `demo1234` | `/dashboard` |
| مدرّس العرض | `teacher@myacademy.edu` | `teacher1234` | `/teacher` |
| ولي الأمر للعرض | `parent@myacademy.edu` | `parent1234` | `/parent` |
| طالب العرض | `student@myacademy.edu` | `student1234` | `/student` |

بعد نجاح الدخول، اختبر أن زيارة رابط طالب تابع لأكاديمية أخرى من جلسة مدير أكاديمية العرض تعيد `404` أو تمنع الوصول، ولا تعرض أي بيانات. راجع `e2e/security.spec.ts` لتنفيذ الاختبار الآلي المقابل.

## 3. قاعدة البيانات وترتيب الترحيلات

ترحيلات SaaS الأساسية مطبقة بالفعل على مشروع `mySAAS`. لا تعِد تشغيلها في الإنتاج من دون مراجعة؛ الغرض من هذا الجدول هو الاحتفاظ بسجل الترتيب التشغيلي.

| الترتيب | الملف | الحالة | الغرض |
|---:|---|---|---|
| 1 | `supabase/saas-foundation.sql` | مطبق | جداول عضويات الأكاديميات والدعوات ودوال التفويض. |
| 2 | `supabase/saas-rls-v2.sql` | مطبق | سياسات عزل القراءة والكتابة متعددة الأكاديميات. |
| 3 | `supabase/saas-security-function-hardening.sql` | مطبق | منع وصول دور الزائر إلى دوال التفويض. |
| 4 | `supabase/saas-security-function-hardening-v2.sql` | مطبق | إلغاء صريح إضافي لصلاحيات الزائر. |
| 5 | `supabase/billing-webhooks.sql` | مطبق | سجل أحداث الفوترة وقيد منع معالجة الاشتراك مرتين. |
| 6 | `supabase/whatsapp-delivery-controls.sql` | مطبق في 13 أغسطس 2026 | موافقة صريحة لولي الأمر وسجل تسليم WhatsApp مع RLS لكل أكاديمية. |
| 7 | `supabase/support-tickets.sql` | مطبق في 13 أغسطس 2026 | تذاكر دعم داخل المنتج مع عزل بالأكاديمية وسياسات قراءة وكتابة حسب الدور. |

تأكد من أن إعدادات Auth في Supabase تسمح بعناوين المنصة الفعلية ضمن **Site URL** و**Redirect URLs**، بما في ذلك نطاق Vercel النهائي وأي نطاق مخصص. استخدم HTTPS حصراً في بيئة الإنتاج.

## 4. إعداد البريد والدعوات

تستخدم المنصة Resend حصراً في `lib/email.ts`. عندما لا توجد قيمة `RESEND_API_KEY`، لن يخرج البريد فعلياً؛ سيُسجل التطبيق المحاولة فقط. لذلك لا تفتح الدعوات للعملاء قبل إتمام الإعداد التالي.

1. أنشئ حساباً في Resend وأنشئ مفتاح API مخصصاً للإنتاج.
2. أضف نطاقاً أو نطاقاً فرعياً مخصصاً للبريد، مثل `mail.your-domain.eg`، ثم انسخ سجلات DNS التي يقدمها Resend وأكمل تحققها. Resend يتطلب نطاقاً مملوكاً ومتحققاً لإرسال البريد، ويوصي بالنطاقات الفرعية لفصل سمعة رسائل النظام عن بقية البريد.[3]
3. في Vercel أضف `RESEND_API_KEY` و`EMAIL_FROM`، مثل `MY Academy <no-reply@mail.your-domain.eg>`، في بيئة Production.
4. أعد النشر، ثم أرسل دعوة اختبار لبريد تملكه وتحقق من وصولها، صلاحية الرابط، وقدرته على إنشاء حساب جديد مرة واحدة فقط.

| المتغير | مثال | المطلوب |
|---|---|---|
| `RESEND_API_KEY` | مفتاح API من Resend | سري وخادمي. |
| `EMAIL_FROM` | `MY Academy <no-reply@mail.example.eg>` | يجب أن يطابق نطاقاً متحققاً. |
| `NEXT_PUBLIC_APP_URL` | `https://app.example.eg` | يُستخدم لإنشاء رابط الدعوة الآمن. |

## 5. إعداد الفوترة

### 5.1 خطة الإطلاق المقترحة

تظهر المنصة الخطط التالية بالعملة المصرية. عدّل الأسعار من خدمة الفوترة فقط إذا تغيرت السياسة التجارية، ثم اختبر مسار الدفع في بيئة اختبار قبل الإنتاج.

| الخطة | السعر الشهري | الاستخدام المستهدف |
|---|---:|---|
| `free` | 0 EGP | التجربة والاستخدام المحدود. |
| `basic` | 299 EGP | أكاديميات صغيرة. |
| `pro` | 799 EGP | أكاديميات نامية. |
| `enterprise` | 1,999 EGP | احتياجات مؤسسية ودعم موسع. |

### 5.2 Paymob — الخيار المصري الأساسي

المنصة تجهز رابط **Unified Checkout** خادمياً وتتحقق من HMAC-SHA512 قبل تسجيل أي عملية. لا تخزن أو تدخل معلومات بطاقات الدفع في MY Academy.

| متغير Paymob | المطلوب |
|---|---|
| `PAYMOB_SECRET_KEY` | مفتاح التاجر السري. |
| `PAYMOB_PUBLIC_KEY` | المفتاح العام المطابق للتاجر. |
| `PAYMOB_PAYMENT_METHODS` | التكاملات/طرق الدفع المسموح بها، مفصولة بفواصل. |
| `PAYMOB_HMAC_SECRET` | سر HMAC الخاص بحساب Paymob. |
| `PAYMOB_HMAC_FIELD_ORDER` | **الترتيب الحرفي** لحقول Transaction Callback من لوحة Paymob. لا تخمّنه ولا تغيّر ترتيبه. |
| `PAYMOB_CHECKOUT_URL_TEMPLATE` | رابط Unified Checkout الخاص بالتاجر، HTTPS، ويحتوي `{client_secret}`. |
| `PAYMOB_BILLING_PHONE` و`PAYMOB_BILLING_CITY` | بيانات افتراضية قانونية ومراجَعة للفوترة. |

أنشئ Webhook في لوحة Paymob يشير إلى:

```text
https://YOUR_DOMAIN/api/billing/paymob/webhook
```

أرسل عملية اختبار من Paymob ثم افحص `billing_events`. يجب أن يكون السجل موثقاً، وأن ينتقل الاشتراك مرة واحدة فقط. لا تعتمد على رابط العودة من صفحة الدفع لتأكيد الاشتراك.

### 5.3 Stripe — خيار اختياري

أضف `STRIPE_SECRET_KEY` و`STRIPE_WEBHOOK_SECRET` و`NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` في Vercel بعد إنشاء حساب Stripe مناسب للنشاط. أنشئ endpoint من لوحة Stripe بعنوان:

```text
https://YOUR_DOMAIN/api/billing/stripe/webhook
```

واشترك في الحدث `checkout.session.completed`. يستقبل Stripe أحداث الويب هوك على endpoint HTTPS ويتطلب من المعالج التحقق من التوقيع باستخدام جسم الطلب الخام وسر endpoint؛ هذا هو النموذج الذي يطبقه المسار الحالي.[2]

> لا تفعّل Stripe وPaymob في واجهة المستخدم إلا بعد نجاح عملية اختبار حقيقية لكل بوابة. وتبقى مفاتيح الأسرار خادمية دائماً.

## 6. اختبارات الإطلاق والقبول

شغّل هذه الأوامر من جذر المشروع بعد أي تغيير برمجي:

```bash
npm run build
npx playwright test e2e/security.spec.ts e2e/billing-webhooks.spec.ts
```

يجب أن تنجح **58 حالة** في مجموعة الرحلات الحالية، أو العدد المعلن في CI إذا أضيفت اختبارات جديدة. تشمل الاختبارات منع تصعيد الدور، عدم إتاحة API التصدير لغير المدير، عزل سجل طالب ومجموعة من أكاديمية ثانية، رفض الدخول الخاطئ، ورفض إشعارات Stripe/Paymob غير الموقعة أو ذات JSON التالف.

| مجال القبول | من ينفذ الاختبار | معيار النجاح |
|---|---|---|
| دخول الأدوار | المالك | كل حساب يصل لبوابته فقط. |
| عزل الأكاديميات | مدير أكاديمية A | لا يمكنه فتح سجل معروف لأكاديمية B أو تصديره. |
| دعوة مستخدم | مدير الأكاديمية | تصل رسالة آمنة، تُستخدم مرة واحدة، وتنتهي صلاحيتها. |
| Paymob | المالك/المالية | توقيع صحيح فقط يفعّل الخطة، ورقم الحدث لا يكرّر التفعيل. |
| Stripe | المالك/المالية | `checkout.session.completed` الموقّع فقط يفعّل الخطة. |
| الانحدار البرمجي | فريق التطوير | البناء وPlaywright ينجحان قبل كل نشر. |

## 7. قائمة تشغيل ما بعد الإطلاق

راقب أول 48 ساعة من الإطلاق يومياً. راجع سجلات Vercel لأخطاء `/api/auth/login` وwebhooks، وتحقق من وصول رسائل الدعوة. لا تسجل رموز الدعوات أو رؤوس التوقيع أو مفاتيح الخدمة في أدوات المراقبة.

| المهمة | التواتر | الإجراء عند الفشل |
|---|---|---|
| سجل أخطاء تسجيل الدخول | يومياً أول أسبوع | تحقق من تطابق متغيرات Supabase و`SESSION_SECRET`، ثم أعد النشر. |
| أحداث الفوترة المعلقة أو المرفوضة | يومياً | راجع توقيع مزود الدفع وحالة العملية من لوحة التاجر، لا تفعل الاشتراك يدوياً بلا إثبات. |
| تسليم البريد | يومياً أول أسبوع | تحقق من DNS وسمعة النطاق وسجلات Resend. |
| اختبار العزل والصلاحيات | قبل كل إصدار | شغل Playwright ثم راجع أي تغيير في سياسات RLS. |
| تدوير الأسرار | فور الاشتباه/وفق سياسة الشركة | غيّر المفتاح في المزود وVercel ثم أعد النشر. |

## 8. الرجوع الآمن عند حدوث مشكلة

إذا تسبب إصدار جديد في مشكلة إنتاجية، استخدم Vercel للرجوع إلى نشر Production سابق معروف وسليم. لا تستخدم الرجوع لحل مشكلة بيانات أو RLS من دون تحليل، لأن الكود الأقدم قد لا يتوافق مع الجداول والترحيلات الحالية. احتفظ بسجل الترحيلات وراجع `SECURITY_VALIDATION_NOTES.md` قبل أي تغيير عاجل.

توقف عن قبول مدفوعات جديدة إذا فشل التحقق من webhooks، ولا تعطل التحقق من التوقيع كحل مؤقت. عالج السبب في المفاتيح أو إعداد endpoint ثم أعد الاختبار.

## 9. مسؤوليات المالك المتبقية

لا يمكن إكمال العناصر التالية من الكود وحده، لأنها تتطلب حسابات تجارية أو ملكية نطاق أو إدارة أسرار. هذه هي التدخلات الوحيدة المتبقية قبل إعلان الإطلاق التجاري.

| الأولوية | المسؤول | المطلوب | الحالة |
|---:|---|---|---|
| 1 | مالك Vercel/Supabase | إبقاء متغيرات Supabase في Vercel موحدة مع مشروع `mySAAS` وإعادة النشر بعد أي تغيير. | تم التحقق في نشر `b34ace8`؛ راقبها عند التعديل. |
| 2 | مالك النطاق/Resend | تحقق نطاق بريد وأضف `RESEND_API_KEY` و`EMAIL_FROM`. | مطلوب قبل دعوات العملاء. |
| 3 | مالك حساب Paymob | أدخل مفاتيح التاجر وHMAC وترتيب الحقول وقالب Checkout، ثم اختبر webhook. | مطلوب قبل تحصيل EGP. |
| 4 | مالك حساب Stripe | أضف مفاتيح Stripe وendpoint إن كان Stripe سيستخدم. | اختياري. |
| 5 | مالك GitHub | ألغِ فوراً رمز GitHub PAT الذي أُرسل سابقاً في المحادثة؛ اعتبره مكشوفاً، ثم راجع GitHub Audit Log وأنشئ رمزاً جديداً بصلاحيات أقل عند الحاجة. | مطلوب فوراً ولم يُنفذ برمجياً. |
| 6 | مالك النطاق | اربط نطاق HTTPS نهائياً واضبط `NEXT_PUBLIC_APP_URL` وURLs في Supabase Auth. | موصى به قبل التسويق العام. |
| 7 | مالك حساب Meta Business | أضف مفاتيح WhatsApp وقوالب Utility معتمدة واضبط webhook؛ اترك `WHATSAPP_MODE` خارج `live` حتى تمر تجربة تسليم حقيقية. | مطلوب قبل إرسال WhatsApp للعملاء. |
| 8 | مالك مبيعات المنصة | أضف `NEXT_PUBLIC_WHATSAPP_SALES_URL` برقم العمل الرسمي بصيغة `https://wa.me/201...` إذا رغب في استقبال طلبات التجربة عبر WhatsApp. | اختياري؛ CTA يبقى آمناً عند غيابه. |

ملاحظة بيانات العرض: حساب المدرس التجريبي مربوط بسجل المدرس وتظهر بوابته الحية بصورة صحيحة بعد الإصلاح. لا توجد مجموعات تجريبية مرتبطة حاليًا، لذلك تظهر أرقام المجموعات والطلاب والحضور بصفر؛ أنشئ مجموعات حقيقية من حساب المدير أو استورد بيانات العميل، ولا تُنشئ بيانات اصطناعية في أكاديمية عميل. حساب ولي الأمر الحي يعرض طفلاً تجريبيًا وملخصات آمنة بقيم صفرية عند غياب السجلات، دون انهيار runtime.

## 12. حزمة النمو التجاري — أغسطس 2026

تم تنفيذ الحزمة التالية في الكود، وهي في انتظار نشر فرع `main` بعد اكتمال التحقق النهائي:

| المجال | ما أُنجز | الضمان أو الحد التشغيلي |
|---|---|---|
| التعريب وRTL | لغة واتجاه الواجهة يعتمدان على تفضيل محفوظ للمستخدم؛ وتم تعريب رحلة onboarding الأساسية. | تبديل اللغة يعيد تحميل بيانات الخادم بالاتجاه الصحيح. |
| تهيئة الأكاديمية | رحلة onboarding تشمل بيانات الأكاديمية، استيراد CSV للطلاب، ودعوة أول مدرس. | تستخدم نظام الدعوات المعزول الحالي ولا تمنح صلاحيات مباشرة. |
| تجربة الهاتف | Manifest وأيقونة PWA وزر تثبيت وعامل خدمي للموارد الثابتة فقط. | لا يخزن العامل الخدمي صفحات أو بيانات أكاديميات خاصة. |
| تقارير ولي الأمر | تقرير ابن محمي بجلسة ولي الأمر مع طباعة أو حفظ PDF عبر المتصفح. | لا يوجد رابط تقرير عام؛ يتحقق الوصول من الأب والأكاديمية والطالب. |
| WhatsApp | موافقة صريحة، سجل حالات تسليم، وضع عرض افتراضي، وقوالب للأحداث التعليمية. | لا يرسل التطبيق شيئاً ما لم توجد المفاتيح **و** تكون `WHATSAPP_MODE=live`. |
| لوحة مالك المنصة | مؤشرات التجارب والتفعيل والتحويل ومخاطر الاحتفاظ فوق بيانات SaaS الفعلية. | الوصول محصور في مالك المنصة. |
| المساعدة والدعم | مركز مساعدة حسب الدور وتذاكر دعم عربية داخل التطبيق. | غير المدير يرى تذاكره فقط؛ والمدير يرى تذاكر أكاديميته فقط. |
| الثقة والبيانات | تصدير JSON مقيد بـ`academy_id` وسياسة خصوصية واحتفاظ محدثة. | يستبعد الأسرار والجلسات والسجلات التشغيلية الحساسة. |
| المبيعات | CTA تجربة اختياري عبر WhatsApp وصفحات عامة أكثر دقة. | لا يظهر رابط المبيعات الحي قبل إضافة رابط المالك في البيئة. |

### تفعيل WhatsApp بعد النشر

أضف المتغيرات التالية في **Vercel Production** فقط، ولا تضعها في المستودع أو في متغيرات `NEXT_PUBLIC_`:

| المتغير | الغرض |
|---|---|
| `WHATSAPP_TOKEN` | رمز System User من Meta Cloud API. |
| `WHATSAPP_PHONE_NUMBER_ID` | معرف رقم الهاتف في WhatsApp Business. |
| `WHATSAPP_API_VERSION` | إصدار Graph API؛ الافتراضي الحالي `v26.0`. |
| `WHATSAPP_APP_SECRET` | سر التطبيق المستخدم للتحقق من توقيع webhook. |
| `WHATSAPP_WEBHOOK_VERIFY_TOKEN` | قيمة عشوائية خاصة للتحقق الأولي من webhook. |
| `WHATSAPP_MODE` | اتركه `demo` أو غائباً أثناء التجارب؛ اجعله `live` فقط بعد اختبار قالب وتسليم صحيحين. |

أنشئ في Meta قوالب **Utility** عربية معتمدة فقط للموعد أو الغياب أو تذكير المستحقات، واحصل على موافقة صريحة من ولي الأمر قبل الإرسال. اربط webhook التالي ثم اشترك في حالات الرسالة فقط:

```text
https://YOUR_DOMAIN/api/webhooks/whatsapp
```

المعالج يتحقق من `x-hub-signature-256`، لا يخزن نص الرسالة الواردة، ويرتب حالات التسليم بطريقة idempotent؛ ولذلك لا تعتبر قبول طلب الإرسال دليلاً على التسليم. اختبر التسليم في رقم داخلي أولاً، ثم فعّل `WHATSAPP_MODE=live` وأعد النشر.

> **تنبيه من فحص Supabase:** الترحيلان الجديدان يملكان RLS وسياسات صريحة. ما زالت أداة الفحص تعرض تحذيرات قديمة لدوال تفويض `SECURITY DEFINER` قابلة للاستدعاء من دور `authenticated` ولخيار حماية كلمات المرور المسربة غير المفعل. لم تُغيّر هذه الدوال خلال الحزمة لأن إلغاء تنفيذها قد يكسر سياسات RLS القائمة. فعّل **Leaked Password Protection** من إعدادات Supabase Auth، ولا تعدل صلاحيات الدوال إلا ضمن مراجعة مستقلة مع اختبارات العزل.

## المراجع

[1]: https://vercel.com/docs/environment-variables/managing-environment-variables "Vercel — Managing environment variables"
[2]: https://docs.stripe.com/webhooks "Stripe — Receive Stripe events in your webhook endpoint"
[3]: https://resend.com/docs/dashboard/domains/introduction "Resend — Verified Domains"


## 10. التسعير الابتدائي — أغسطس 2026

تم اعتماد تسعير مبدئي قابل للتعديل من ملف `services/saas.ts`، والأسعار شهرية بالجنيه المصري:

| الخطة | السعر الشهري | الطلاب | المدرسون | المجموعات | الأكاديميات | التخزين |
|---|---:|---:|---:|---:|---:|---:|
| مجاني | 0 EGP | 50 | 3 | 5 | 1 | 250 MB |
| أساسي | 399 EGP | 200 | 10 | 25 | 1 | 2 GB |
| احترافي | 899 EGP | 750 | 35 | 100 | 3 | 10 GB |
| مؤسسات | 2,499 EGP | 5,000 | 200 | 500 | 10 | 50 GB |

هذه أرقام إطلاق أولية قابلة للمراجعة بعد أول عملاء. حدود الطلاب والمدرسين والمجموعات تدخل في منطق الخادم، بينما حدود الأكاديميات والتخزين معروضة حاليًا كحدود تجارية للخطة وتحتاج طبقة قياس واستهلاك مستقلة قبل فرضها آليًا. لتعديل الأسعار أو الحدود، عدّل `PLANS` في `services/saas.ts` ثم نفّذ فحص TypeScript وbuild قبل النشر.

يفضل اختبار الأسعار مع أول 3–5 أكاديميات وقياس التحويل والإلغاء وتكلفة الدعم قبل تثبيت الأسعار نهائيًا. لا يتم تفعيل الدفع الحقيقي قبل إدخال مفاتيح Stripe أو Paymob التجارية واختبار Webhook ناجح.

## 11. تحسينات onboarding وAnalytics وتنظيف العرض

- تم تنظيف أولياء الأمور التجريبيين ذوي النطاق `@parent.local` من الأكاديمية التجريبية فقط. النتيجة المتحققة: صفر سجلات وهمية متبقية، وبقاء حساب `parent@myacademy.edu` الأساسي، وعدم وجود عضويات وهمية يتيمة.
- تم تحسين استيراد CSV للطلاب لدعم الخلايا المقتبسة والفواصل داخل الأسماء، مع حد أقصى 1000 طالب في العملية، ومنع تكرار الطلاب، وإعادة استخدام ولي الأمر الموجود بدل إنشاء سجل جديد لكل صف.
- تم ربط إكمال onboarding بحدث `onboarding.complete` وإضافة رابط مباشر إلى `/students/import`.
- تمت إضافة funnel داخلي في `/analytics` يقيس التسجيل، إكمال onboarding، إنشاء وقبول الدعوات، بدايات الدفع، التحويلات المدفوعة، الاشتراكات النشطة، والإلغاءات المسجلة على الاشتراك.
- يعتمد قياس الإلغاء على حالة الاشتراك `canceled` أو `cancel_at_period_end` أو `canceled_at`. ربط تنفيذ إلغاء الاشتراك لدى مزود الدفع نفسه يحتاج تفعيل مفاتيح Paymob/Stripe وربط واجهة الإلغاء لاحقًا.
