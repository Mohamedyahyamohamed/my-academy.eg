# مراجعة شاملة لمنطق الأدوار والصلاحيات — MY Academy

**تاريخ المراجعة:** 13 أغسطس 2026
**النطاق:** النسخة الحالية من الفرع `main`، عند الالتزام [`134b4a6`](https://github.com/Mohamedyahyamohamed/my-academy.eg/commit/134b4a6).
**الأدوار محل المراجعة:** مدير الأكاديمية، المدرس، الطالب، وولي الأمر.

## الملخص التنفيذي

قاعدة المشروع جيدة من ناحية فصل بوابات المستخدمين ووجود `requireRole`، كما أن الإصلاح الأخير أغلق وصولًا مباشرًا كان ظاهرًا إلى الإعدادات والمدفوعات. لكن النموذج الحالي ما زال **RBAC على مستوى الدور فقط** في عدد كبير من العمليات، وليس **تفويضًا قائمًا على الدور والملكية والعلاقة**. لذلك يمكن أن ينجح منع المدرس من صفحة مالية، بينما تظل هناك إجراءات كتابة أخرى تسمح له بتمرير `studentId` أو `lessonId` أو `groupId` لا يخصه.

> **القرار المقترح:** اعتبر الصلاحية قاعدة موحّدة على الخادم وقاعدة البيانات، لا قرارًا موزعًا بين الصفحة والخدمة وواجهة المستخدم. يجب أن يتأكد كل تعديل من ثلاثة عناصر: هوية الفاعل، الأكاديمية المنتمى إليها، وملكيته الفعلية للمورد أو علاقته به.

أخطر نقطة مكتشفة هي أن الجلسة الحالية تعتمد على ملف تعريف مشفّر بـ Base64 فقط، وليس موقّعًا أو متحققًا منه لكل طلب. هذه القيمة تتضمن الدور و`academy_id`، وتستخدمها `getCurrentUser()` مباشرة عند فرض الصلاحيات. حتى مع وجود Supabase، فإن الكود يُصدر ملف تعريف `ma_session` ويعيد الاعتماد عليه داخل الإجراءات. [1] [2]

| التقييم | النتيجة |
|---|---|
| تصميم البوابات | جيد؛ لكل دور مسار دخول واضح وواجهة مستقلة. |
| منع الوصول المباشر للصفحات الإدارية | تحسن ملحوظ بعد الالتزام الأخير، لكنه ليس كافيًا وحده. |
| حماية إجراءات الكتابة | تحتاج إعادة هيكلة؛ معظمها يتحقق من الدور ولا يثبت الملكية. |
| عزل المستأجرين والأكاديميات | توجد طبقة أولية، لكن مسار الرجوع إلى الذاكرة عند فشل RLS غير آمن. |
| اختبار الأدوار | يوجد أساس مفيد، لكن الاختبارات لا تغطي أغلب هجمات تغيير المعرّفات أو كل إجراءات الخادم. |

## ما راجعته

راجعت طبقة الجلسة، تعريفات الأدوار، تحميل بيانات البوابات، طبقة النطاق المشتركة، إجراءات الطلاب والحضور والواجبات، واجهة البحث، واختبارات الأمان والرحلات. شُغّلت اختبارات Playwright الخاصة بالأمان والرحلات الأربعة؛ كانت النتيجة **29 ناجحًا و15 فاشلًا**. أغلب حالات الفشل المتبقية تبدو مشاكل اختبار أو توطين، مثل انتظار عناوين إنجليزية بينما الواجهة عربية، واستخدام `expect(...).or` بصيغة غير مدعومة. لذلك لا يجب اعتبار هذه الحالات دليلًا تلقائيًا على عطل في المنتج، لكنها تُضعف الثقة في شبكة الاختبارات الحالية. [11]

## نتائج المراجعة حسب الخطورة

| الأولوية | النتيجة | التأثير | التوصية المباشرة |
|---|---|---|---|
| حرجة | ملف الجلسة `ma_session` يحمّل JSON مشفّرًا بـ Base64 فقط ويثق في الدور و`academy_id` الواردين منه. | يمكن نظريًا انتحال دور أو أكاديمية عند إرسال Cookie مُصطنع؛ لا يوجد توقيع أو تحقق من Supabase في `getCurrentUser()`. | أوقف استخدام هذا الملف كمصدر حقيقة في الإنتاج. استخرج المستخدم من `supabase.auth.getUser()` في كل طلب، ثم اقرأ `profiles` من قاعدة البيانات وتحقق من `is_active`. |
| حرجة | `fetchTableRLS` يعود إلى `collections()` عند خطأ Supabase أو خطأ RLS. | فشل قاعدة البيانات أو السياسة قد يتحول إلى تسريب بيانات من المخزن المحلي بدل فشل آمن. | في الإنتاج، أعد خطأ مضبوطًا أو قائمة فارغة مع 5xx/403 حسب الحالة؛ اجعل fallback محصورًا في وضع العرض المحلي المصرح به فقط. |
| عالية | إجراءات كتابة الطلاب تسمح للمدرس بإنشاء وتعديل وأرشفة أي طالب، بينما الخدمة لا تثبت أن الطالب أو المجموعات تخص المدرس. | يمكن للمدرس تعديل بيانات طالب أو إعادة تعيين مجموعاته خارج نطاقه بمجرد تمرير ID صحيح. | قبل كل كتابة: `assertTeacherCanManageStudent(actor, studentId)` و`assertTeacherCanManageGroups(actor, groupIds)`؛ أو قصر إنشاء/أرشفة الطالب على الأدمن. |
| عالية | تسجيل الحضور يسمح للمدرس بتمرير أي `lessonId` و`studentId`، ولا يوجد تحقق من ملكية الحصة أو عضوية الطالب في المجموعة داخل الإجراء. | يستطيع المدرس تسجيل حضور طالب في حصة ليست له إذا عرف المعرّفات. | تحقق من أن الحصة في نطاق مجموعات المدرس، وأن الطالب عضو في مجموعتها، قبل استدعاء الخدمة. |
| عالية | تسليم الواجب يأخذ `studentId` من العميل، ومراجعة التسليم لا تتحقق من أن الواجب ضمن مجموعات المدرس. | الطالب قد يحاول التسليم باسم طالب آخر، والمدرس قد يراجع واجبًا خارج نطاقه. | استنتج الطالب من الجلسة بدل طلب `studentId`، وتحقق من `homework → group → teacher/assistant` عند المراجعة. |
| عالية | البحث يعامل المدرس مثل الأدمن ويُرجع بحثًا على مستوى الأكاديمية بالكامل. | إفصاح غير لازم عن طلاب ومجموعات لا يدرّسها المستخدم. | اجعل نتائج المدرس محصورة في طلاب ومجموعات `teacherGroupScope` أو في الصلاحيات الممنوحة للمساعد. |
| عالية | ربط الطالب وولي الأمر يعتمد جزئيًا على البريد الإلكتروني والمخزن المحلي بدل `profile_id` وعلاقات قاعدة البيانات. | تغيّر البريد أو وجود بيانات قديمة قد يكسر البوابة أو يخلط الهوية بالعلاقة التعليمية. | استخدم جداول ربط ثابتة: `student_profiles(student_id, profile_id)` و`parent_students(parent_id, student_id)`، واعتمد على `auth.uid()` فقط. |
| متوسطة | `currentAcademyId()` يعود إلى أول أكاديمية عند غياب الجلسة. | أي استدعاء غير محمي أو مهمة خلفية قد يعمل في أكاديمية غير مقصودة. | اجعل الأكاديمية مطلبًا صريحًا في سياق الطلب، واسمح بالـ fallback فقط داخل أدوات seed/test المعزولة. |
| متوسطة | الدور Enum واحد لكل ملف تعريف، مع استثناء `SUPER_ADMIN` الذي يتخطى كل القيود. | يصعب دعم مستخدم هو مدرس وولي أمر، أو مدير مالي محدود، أو مساعد بحزمة صلاحيات صغيرة. | استخدم عضويات وصلاحيات منفصلة عن الملف الشخصي: `academy_memberships` و`role_assignments` و`permissions`. |
| متوسطة | رفع ملفات الواجب يقبل `homeworkId` و`studentId` مباشرة ويرفع عبر عميل خدمة. | قد تُحفظ ملفات في مسار واجب أو طالب آخر من دون إثبات العلاقة. | اشتق الطالب من الجلسة، تحقق من العضوية في الواجب، واستخدم مسارات Storage لا تسمح إلا بـ `academy/student/homework` مع سياسة تحقق. |
| متوسطة | تسجيل الدخول لا يظهر تحققًا من `profiles.is_active` بعد جلب الملف الشخصي. | الحساب المعطل أو المؤرشف قد يظل قادرًا على الدخول إن بقيت بيانات Supabase صحيحة. | ارفض الدخول عند `is_active = false`، وألغِ جلسات المستخدم عند التعطيل. |
| متوسطة | الحسابات التجريبية وكلمات المرور الافتراضية معرفة في الكود. | خطر تشغيل وضع العرض أو كلمات المرور المعروفة في بيئة إنتاج غير مهيأة أو عند خطأ إعداد. | افصل وضع العرض عبر متغير بيئة إلزامي `DEMO_MODE=true`، ولا تشحن حسابات أو كلمات مرور تجريبية في إنتاج حقيقي. |
| منخفضة | إشعار واجب جديد يستدعي دالة اسمها `sendPaymentReminder` ويشير رابط الإشعار إلى بوابة الطالب للوالد. | منطق الإشعار غير معبر وقد يربك المستلم أو يصعّب التتبع. | افصل قنوات ورسائل الواجبات عن المدفوعات، ووجّه ولي الأمر إلى `/parent/homework`. |

## صورة الصلاحيات المستهدفة

ينبغي أن تكون الصلاحية مزيجًا من **الدور** و**العضوية في الأكاديمية** و**العلاقة بالمورد**. الجدول التالي يحدد نموذجًا بسيطًا يمكن البناء عليه.

| المورد / العملية | مدير الأكاديمية | المدرس الأساسي | المساعد | الطالب | ولي الأمر |
|---|---|---|---|---|---|
| إعدادات الأكاديمية والمستخدمون والاشتراك | كامل | ممنوع | ممنوع | ممنوع | ممنوع |
| طلاب الأكاديمية | كامل | قراءة طلاب مجموعاته فقط؛ تعديل ملاحظات تعليمية محددة | حسب المجموعة | بياناته فقط | بيانات الأطفال المرتبطين فقط |
| المجموعات | كامل | إدارة المجموعات المسندة له | قراءة أو حضور فقط حسب التفويض | قراءة المجموعات المنضم لها | قراءة مجموعات الطفل |
| الحصص والحضور | كامل | إنشاء وتعديل حصص مجموعاته وتسجيل حضورها | تسجيل حضور المجموعات المفوضة | check-in لحصصه فقط | قراءة حضور الطفل فقط |
| الواجبات والتصحيح | كامل | إنشاء وتصحيح واجبات مجموعاته | وفق صلاحية صريحة | تسليم واجباته فقط | قراءة حالة الطفل فقط |
| الاختبارات والدرجات | كامل | إنشاء وإدخال درجات مجموعاته | قراءة أو إدخال حسب التفويض | قراءة درجاته فقط | قراءة درجات الطفل فقط |
| المدفوعات والتصدير والتقارير المالية | كامل أو مدير مالي مخصص | ممنوع افتراضيًا | ممنوع | قراءة حالة مدفوعاته فقط إن تقرر | قراءة حالة الطفل فقط |
| الرسائل | كامل ضمن الأكاديمية | طلاب مجموعاته وأولياؤهم فقط | النطاق المفوض فقط | المعلم/الإدارة وفق قواعد المنصة | إدارة الطفل ومعلميه فقط |

## التصميم المقترح للتفويض

### 1. اجعل هوية الفاعل موثقة وقصيرة العمر

ينبغي أن تصبح دالة شبيهة بالتالي نقطة الدخول الوحيدة لجميع صفحات الخادم وServer Actions وAPI Routes:

```ts
async function getActor(): Promise<Actor> {
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) throw new UnauthorizedError();

  const profile = await profilesRepository.byAuthId(user.id);
  if (!profile || !profile.is_active) throw new ForbiddenError();

  return {
    authUserId: user.id,
    profileId: profile.id,
    academyId: profile.academy_id,
    roles: profile.roles,
  };
}
```

لا تستخدم `academy_id` أو `role` صادرين من Cookie مخصص. وإذا بقي ملف Session لتحسين واجهة المستخدم، فليكن مشتقًا من الجلسة الحقيقية وموقّعًا، وألا يستخدم لاتخاذ قرار صلاحية.

### 2. أنشئ Authorizer مركزيًا قابلًا للاختبار

بدل تشتيت `requireRole("ADMIN", "TEACHER")` في الإجراءات، استخدم دوال خاصة بالمورد. أمثلة عملية:

```ts
await authorizer.assertAcademyAdmin(actor);
await authorizer.assertTeacherCanAccessGroup(actor, groupId);
await authorizer.assertTeacherCanManageStudent(actor, studentId);
await authorizer.assertStudentIsSelf(actor, studentId);
await authorizer.assertParentCanAccessChild(actor, studentId);
```

هذه الدوال يجب أن تعتمد على استعلامات قاعدة البيانات ونطاق الأكاديمية، لا على `collections()` في بيئة Supabase. وبذلك يصبح الخطأ 403 موحّدًا وقابلًا للتدقيق، ويمكن تغطية التفويض نفسه باختبارات وحدة مستقلة.

### 3. افصل الدور عن العلاقة

اقتراح مخطط مبسط:

| الجدول | الهدف |
|---|---|
| `profiles` | هوية المستخدم، حالة الحساب، الأكاديمية الأساسية. |
| `academy_memberships` | عضوية المستخدم في أكاديمية ودوره/أدواره فيها. |
| `teacher_profiles` | ربط `profile_id` بسجل المدرس. |
| `student_profiles` | ربط الطالب بحساب الدخول، بدل المطابقة بالبريد. |
| `parent_profiles` | ربط ولي الأمر بحسابه. |
| `parent_students` | ربط ولي الأمر بأكثر من طفل ونوع العلاقة وصلاحية الاطلاع. |
| `group_staff` | المدرس الأساسي والمساعدون مع صلاحيات دقيقة لكل مجموعة. |
| `role_permissions` | خريطة الصلاحيات القابلة للتوسع مثل `payments.read` و`attendance.write`. |

هذا يسمح مثلًا بأن يكون المستخدم مدرسًا وولي أمر في وقت واحد، وأن يمنح المساعد صلاحية الحضور فقط من دون التقييم أو الرسائل.

### 4. ضع RLS في قاعدة البيانات كطبقة إلزامية

يجب أن تمنع RLS أي استعلام أو تعديل غير مصرح به حتى إن تم تجاوز الواجهة أو Server Action. أمثلة للمبدأ:

- الطالب يرى صفه في `students` فقط عندما يطابق `student_profiles.profile_id = auth.uid()`.
- ولي الأمر يرى الطالب فقط عند وجود صف في `parent_students` مربوط بـ `auth.uid()`.
- المدرس يرى الحصص والطلاب فقط عند وجود `group_staff` أو عند تطابق `groups.teacher_id` مع مدرسه.
- المدفوعات والإعدادات والتصدير تقصر على دور إداري أو صلاحية مالية صريحة في `academy_memberships`.

مع هذا التصميم، لا تجعل فشل RLS سببًا للرجوع إلى Cache. الفشل يجب أن يكون ظاهرًا ومراقبًا، لا أن ينتج بيانات بديلة.

## التحسينات التفصيلية حسب الدور

### مدير الأكاديمية

مدير الأكاديمية يحتاج صلاحيات واسعة، لكن ليس من الضروري أن تكون كل الصلاحيات مالية وتشغيلية في دور واحد. أوصي بإضافة أدوار فرعية، مثل `ACADEMY_ADMIN` و`FINANCE_MANAGER` و`OPERATIONS_MANAGER`، مع بقاء إدارة المستخدمين وتغيير إعدادات الأكاديمية مقصورة على المدير. كل عملية إدارية حساسة، كإنشاء مستخدم أو تعديل رسوم أو تصدير بيانات، يجب أن تسجل في Audit Log يشمل الفاعل، المورد، القيمة السابقة والجديدة، IP/معرف الطلب إن توفر، والنتيجة.

ينبغي أن تتحول عملية إنشاء حساب الطالب أو ولي الأمر من كلمة مرور افتراضية إلى **دعوة واحدة محددة الصلاحية ومنتهية**. تُرسل الدعوة إلى البريد أو رقم الهاتف، ثم يختار المستخدم كلمة مروره. لا ينبغي أن يعرض النظام كلمات مرور افتراضية أو يحتفظ بها في الكود.

### المدرس والمساعد

المدرس ليس أدمن محدودًا؛ هو مالك تعليمي لمجموعات محددة. كل إجراء للمدرس يجب أن يبدأ بإثبات ملكية المجموعة أو وجوده في `group_staff`. ويفضل تحديد صلاحيات المساعد لكل مجموعة: `attendance.write` و`homework.review` و`grades.write` و`messages.send`، بدل مساواة المساعد بالمدرس الأساسي.

في المشروع الحالي، يجب إعادة بناء إجراءات الطلاب والحضور والواجبات والدرجات حول نطاق المجموعة. عند إضافة طالب إلى مجموعة أو إزالته منها، يجب التحقق من أن المجموعة ضمن نطاق المدرس. وعند تصحيح واجب أو إدخال درجة، يجب التحقق من أن الطالب مسجل في المجموعة التي ينتمي إليها الامتحان أو الواجب.

### الطالب

الطالب لا يمرر `studentId` في أي عملية قابلة للتعديل. الخادم يحدده من `auth.uid()` ثم يثبت أن الواجب أو الحصة أو الدرجة تخصه. يسمح للطالب بالقراءة، التسليم، وطلب check-in في الحصص المؤهلة فقط. ويمكن إضافة حالات عمل واضحة للواجب: `ASSIGNED → SUBMITTED → RETURNED → GRADED`، مع قواعد تمنع التعديل بعد الإغلاق إلا بإذن المدرس.

تظهر بيانات الطالب وملفات الواجبات ضمن مسارات تخزين مملوكة له، وتكون السياسة مرتبطة بـ `auth.uid()` و`student_id` المستخلص من الحساب، لا من قيمة FormData.

### ولي الأمر

ولي الأمر قد يرتبط بأكثر من طفل، لذلك لا يكفي تمرير `childId` من رابط الصفحة. أي صفحة فرعية مثل الحضور أو الدرجات أو المدفوعات يجب أن تنفذ `assertParentCanAccessChild(actor, childId)` قبل القراءة. ولأسباب خصوصية، أوصي بمستويات رؤية محددة: عرض الدرجات، عرض الحضور، عرض الرسائل، استلام الإشعارات، ودفع الرسوم. بهذه الطريقة يمكن التعامل مع حالات الأب والأم والوصي دون مشاركة نطاق غير مقصود.

## خارطة تنفيذ مقترحة

| المرحلة | العمل | الأولوية | المخرج القابل للقياس |
|---|---|---:|---|
| P0 | إزالة الثقة في Cookie Base64، واعتماد جلسة Supabase + ملف فعال من قاعدة البيانات. | حرجة | لا يمكن تغيير الدور/الأكاديمية عبر Cookie يدوي. |
| P0 | إيقاف fallback إلى Cache عند خطأ Supabase/RLS في الإنتاج. | حرجة | فشل قاعدة البيانات يؤدي إلى 403/5xx منضبط وليس بيانات بديلة. |
| P0 | إنشاء `authorizer` وربط إجراءات الطلاب والحضور والواجبات والدرجات والرفع به. | حرجة | كل عملية كتابة تتحقق من الدور والملكية والعلاقة. |
| P1 | استبدال ربط البريد بعلاقات `profile_id` و`parent_students` و`student_profiles`. | عالية | يمكن تغيير البريد بلا كسر البوابات أو التفويض. |
| P1 | تضييق البحث والرسائل والتقارير إلى نطاق المدرس/الطفل. | عالية | المدرس لا يرى إلا طلاب مجموعاته. |
| P1 | تطبيق RLS متكامل واختبارات ضد قاعدة Supabase حقيقية مع مستخدمين متعددين. | عالية | منع عبر قاعدة البيانات، لا عبر الواجهة فقط. |
| P2 | اعتماد عضويات متعددة وصلاحيات دقيقة للمساعدين والإدارة المالية. | متوسطة | دعم مدرس+ولي أمر ومدير مالي ومساعد محدود الصلاحية. |
| P2 | تحويل إنشاء الحسابات إلى دعوات وتطبيق MFA اختياري للإدارة. | متوسطة | لا كلمات مرور افتراضية، مع دورة وصول قابلة للتدقيق. |
| P2 | توحيد Audit Log والإشعارات والـ observability. | متوسطة | سجل كامل للعمليات الحساسة والتنبيهات الفاشلة. |

## خطة اختبار يجب إضافتها

لا تكفِ اختبارات زيارة الصفحات. أضف مصفوفة تفويض تستدعي المسارات والإجراءات مباشرة مع معرفات من نفس الأكاديمية وأكاديمية مختلفة:

| حالة الاختبار | النتيجة المطلوبة |
|---|---|
| مدرس يحاول تعديل طالب خارج مجموعاته | `403` ولا تغيير في قاعدة البيانات. |
| مدرس يحاول إضافة طالب إلى مجموعة يملكها مدرس آخر | `403`. |
| مدرس يحاول تسجيل حضور طالب غير مسجل في الحصة | `403` أو `422`. |
| طالب يرسل واجبًا مع `studentId` لطالب آخر | `403`، والخادم يستخدم الطالب من الجلسة فقط. |
| ولي أمر يفتح درجات طفل غير مرتبط به | `404` أو `403` من قاعدة البيانات والواجهة. |
| مستخدم معطل يحاول تسجيل الدخول أو استخدام جلسة قديمة | `401/403` وجلسة ملغاة. |
| مدرس يبحث عن اسم طالب خارج مجموعاته | لا تظهر أي نتيجة. |
| طلب RLS يفشل | لا يوجد fallback إلى Cache في الإنتاج. |
| مدير من أكاديمية A يطلب موردًا من B | `404/403` من كل API وServer Action. |
| مساعد يملك الحضور فقط ويحاول إدخال درجات | `403`. |

ينبغي أن تُشغّل هذه الحالات ضد مشروع Supabase اختباري أو بيئة Preview متصلة بقاعدة بيانات معزولة. تجنب الاعتماد فقط على وضع الذاكرة المحلي، لأنه لا يثبت فعالية RLS أو ملفات الجلسة أو العلاقات الحقيقية.

كذلك، أصلح اختبارات الواجهة لتستخدم `data-testid` أو أدوار الوصول ونصوصًا عربية ثابتة، بدل عناوين إنجليزية أو استخدام API غير مدعوم مثل `expect(...).or`. فصل اختبار الصلاحية عن اختبار الترجمة سيجعل سبب الفشل واضحًا.

## تحسينات سبق تطبيقها ولا ينبغي التراجع عنها

الالتزام الحالي أغلق وصول المدرس إلى `/payments` ووصول الطالب وولي الأمر إلى `/settings`، وأضاف اختبارات مباشرة لهذه المسارات. هذه خطوة صحيحة، لكنها تمثل **طبقة واجهة ومسار** وليست حلًا كاملًا لملكية الموارد داخل الأفعال. [12]

## أسئلة جيدة يمكن توجيهها إلى ارينا

لأن ارينا عملت سابقًا على المشروع، هذه الأسئلة ستختصر وقت اتخاذ القرار:

1. هل Supabase هو مصدر الحقيقة الفعلي للإنتاج حاليًا، أم ما زال وضع `collections()` مستخدمًا في Vercel؟
2. هل توجد سياسات RLS كاملة ومفعّلة لكل الجداول الفرعية، خصوصًا `attendance` و`grades` و`homework_submissions` و`payments` وStorage؟
3. هل المطلوب أن يحرر المدرس بيانات الطالب الأساسية، أم فقط بياناته التعليمية داخل مجموعاته؟
4. ما حدود المساعد تحديدًا: حضور فقط، أم واجبات ودرجات ورسائل أيضًا؟
5. هل يمكن للمستخدم امتلاك أكثر من دور في أكاديمية واحدة أو في أكاديميات مختلفة؟
6. هل المقصود من حسابات العرض أن تعمل على الإنتاج دائمًا، أم يجب عزلها عن بيانات العملاء الفعلية؟
7. هل توجد آلية دعوات أو استعادة حسابات مخطط لها بدل كلمات المرور الافتراضية؟

## الخلاصة

أفضل ترتيب عملي هو: **تأمين مصدر الجلسة أولًا، ثم توحيد التفويض حول الملكية، ثم جعل RLS طبقة إلزامية، ثم تحسين نموذج العضويات والاختبارات**. لا أوصي بإضافة مزايا جديدة في التدفقات التعليمية قبل تنفيذ P0؛ لأن اتساع المنطق الحالي سيجعل معالجة الصلاحيات لاحقًا أصعب وأكثر عرضة للأخطاء.

## مراجع الكود

[1]: https://github.com/Mohamedyahyamohamed/my-academy.eg/blob/main/services/session.ts#L45-L76 "Session service — current user and role checks"
[2]: https://github.com/Mohamedyahyamohamed/my-academy.eg/blob/main/app/api/auth/login/route.ts#L62-L81 "Login route — ma_session issuance"
[3]: https://github.com/Mohamedyahyamohamed/my-academy.eg/blob/main/services/_shared.ts#L29-L53 "Shared data access — RLS fallback behavior"
[4]: https://github.com/Mohamedyahyamohamed/my-academy.eg/blob/main/services/students.ts#L343-L493 "Student writes and group assignments"
[5]: https://github.com/Mohamedyahyamohamed/my-academy.eg/blob/main/app/actions/students.ts#L8-L66 "Student server actions"
[6]: https://github.com/Mohamedyahyamohamed/my-academy.eg/blob/main/app/actions/attendance.ts#L27-L63 "Attendance server actions"
[7]: https://github.com/Mohamedyahyamohamed/my-academy.eg/blob/main/app/actions/homework.ts#L43-L77 "Homework review and submission actions"
[8]: https://github.com/Mohamedyahyamohamed/my-academy.eg/blob/main/app/api/search/route.ts#L21-L56 "Role-scoped search route"
[9]: https://github.com/Mohamedyahyamohamed/my-academy.eg/blob/main/services/portals.ts#L16-L31 "Parent and student identity resolution"
[10]: https://github.com/Mohamedyahyamohamed/my-academy.eg/blob/main/app/actions/upload.ts#L8-L40 "Homework upload action"
[11]: https://github.com/Mohamedyahyamohamed/my-academy.eg/tree/main/e2e "Current Playwright role and security tests"
[12]: https://github.com/Mohamedyahyamohamed/my-academy.eg/commit/134b4a6 "Role-access and demo-portal fixes"
