# فحص حي أولي — 13 أغسطس 2026

## المسارات التي تم فحصها

| المسار | النتيجة | ملاحظات |
|---|---|---|
| `/parent` | يعمل | لوحة ولي الأمر تعرض بيانات الطفل وروابط الحضور والواجبات والدرجات والمصاريف والرسائل والإشعارات والدعم. رُصد عرض `EGP 0` بالإنجليزية في قيم المصاريف؛ يحتاج التحقق من نشر تعريب المنسقات المركزية أو معالجة مسار العرض المتبقي. |
| `/pricing` | يعمل | الخطط الأربع والأسعار وحدود الطلاب والمدرسين والمجموعات والتخزين تظهر بالعربية وبالجنيه المصري، وروابط البدء تعمل ظاهريًا. |
| `/admin` | غير موجود عمدًا | يعيد 404؛ لوحة المدير تعتمد المسار `/dashboard` وليس `/admin`. |
| تسجيل الخروج | يعمل | قائمة الحساب تسجل الخروج بنجاح وتعيد المستخدم إلى صفحة الدخول، التي تعرض بيانات التجربة بوضوح. |
| `/dashboard` (مدير) | يعمل | تسجيل الدخول التجريبي يعيد إلى لوحة المدير الصحيحة. تظهر مؤشرات الطلاب والمجموعات والمالية، والتنقل التشغيلي والتقارير والخصوصية والدعم. لا توجد عمليات مالية فعلية في بيانات العرض. ما زال `EGP` إنجليزيًا في القيم لأن تعديل `formatCurrency` موجود محليًا ولم يُنشر بعد. |
| `/groups` (مدير) | يعمل | الحالة الفارغة مترجمة وتوضح نقطة البداية، وزر إنشاء مجموعة متاح. لم يتم إنشاء بيانات جديدة في بيئة العرض. |
| `/students` (مدير) | يعمل | جدول الطلاب والبحث والفلاتر والاستيراد وإجراءات العرض والتعديل والأرشفة تعمل وتظهر 19 طالبًا. بقيت عبارة الترقيم `Showing 1–8 of 19` وزر `Next` بالإنجليزية؛ يلزم تعريبهما. |
| `/privacy` (مدير) | يعمل | سياسة الخصوصية العربية تعرض حدود المعالجة وRLS والتصدير، ورابط تنزيل نسخة بيانات الأكاديمية ظاهر. لم يُنزّل الأرشيف لتجنب نسخ بيانات شخصية من الإنتاج أثناء الفحص. يلزم اعتماد الصياغة النهائية مع مختص قانوني قبل الإطلاق التجاري. |
| `/support` (مدير) | يعمل | نموذج التذكرة مع تصنيف وعنوان وتفاصيل وسجل التذاكر يعمل؛ السجل يظهر فارغًا في البيئة التجريبية. لم تُرسل تذكرة اختبار حتى لا نضيف ضوضاء إلى دعم الإنتاج. |
| `/teacher` (مدرّس) | يعمل | تسجيل الدخول يوجّه إلى البوابة المقيدة بالمدرس، وتعرض روابط المجموعات والحصص والحضور والواجبات والدرجات والطلاب والمساعدين. لا توجد مجموعات أو طلاب مسندون لهذا الحساب التجريبي، لذلك ظهرت الحالات الفارغة الصحيحة ولم يمكن اختبار إدخال حضور أو درجات على سجل حقيقي دون إنشاء بيانات جديدة. |
| حماية `/dashboard` من المدرّس | يعمل | محاولة فتح لوحة المدير مباشرة من جلسة المدرس أعادت التوجيه تلقائيًا إلى `/teacher` ولم تمنح أي بيانات أو أدوات إدارية. |
| `/help` (مدرّس) | يعمل | يعرض الدليل العربي الملائم للمدرس بالترتيب التشغيلي للمجموعات والحضور والواجبات والدرجات، مع روابط صحيحة للمسارات المتاحة لهذا الدور وإرشادات خصوصية ودعم. |
| `/student` (طالب) | يعمل مع ملاحظة تحميل أولي | في أول عرض بعد تسجيل الدخول ظهرت رسالة «الملف غير مرتبط»، لكن محاولة فتح `/dashboard` أعادت التوجيه الآمن إلى `/student` ثم حمّلت ملف سارة ولوحة الطالب والدرجات والحصص والواجبات والتقدم كما يجب. لا يوجد تسريب صلاحيات، لكن وميض الحالة غير المرتبطة بعد دخول جديد يستحق الفحص في منطق جلب الملف. |
| حماية `/dashboard` من الطالب | يعمل | محاولة فتح لوحة المدير مباشرة من جلسة الطالب أعادت التوجيه تلقائيًا إلى `/student` ولم تمنح أي بيانات أو أدوات إدارية. |

## ملاحظة

تبديل الجلسات بين الأدوار يعمل عبر تسجيل الخروج. اكتمل فحص المدرس والطالب ومركز المساعدة الخاص بالمدرس؛ ستتم معالجة ملاحظة التحميل الأولي للطالب مع عيوب التعريب قبل التحقق الختامي.

*هذا الملف محلي للتوثيق أثناء الاختبار، ولا يُنشر تلقائيًا.*


## إعادة التحقق بعد نشر التعريب

تم نشر الالتزام `a69fa8b5325e4a323d3a419010215a7830c08f15` بعنوان `fix: localize shared currency and pagination` بنجاح على الإنتاج، وحالة النشر `READY`.

أُعيد فتح بوابة الطالب الحية بعد ملاحظة تحميل عابر سابقة. ظهرت البوابة بصورة سليمة باسم **سارة التجريبية**، مع بطاقة رمز QR ومؤشرات الحضور ومتوسط الدرجات والمجموعات والواجبات ولوحة المتصدرين، ولم تعد تظهر رسالة «الملف غير مرتبط». الحساب مرتبط فعليًا بسجل طالب وعضوية نشطة، ولا تظهر له سوى بيانات أكاديميته.

عدم وجود مجموعات أو حصص أو درجات أو واجبات لهذا الحساب التجريبي حاليًا يؤدي إلى حالات فارغة متوقعة، وليس إلى عطل تشغيلي. كما تم التحقق من وجود متغيرات اتصال قاعدة البيانات المطلوبة في بيئة الإنتاج والمعاينة، ولم تظهر أخطاء تشغيل لمسار `/student` أثناء التحقق.


أُعيد فحص `/parent` بعد النشر بحساب ولي الأمر. قيمة الرصيد المتبقي تظهر الآن بصيغة عربية سليمة: **`٠ ج.م.`** في مؤشرات اللوحة وبطاقة الطالبة، وبذلك أُغلقت ملاحظة ظهور `EGP 0` الإنجليزية السابقة.
