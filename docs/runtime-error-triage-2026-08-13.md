# فرز أخطاء التشغيل — 13 أغسطس 2026

## مصدر البيانات

استعلام أخطاء تشغيل Vercel للإنتاج خلال آخر 7 أيام في 13 أغسطس 2026.

| الخطأ | آخر ظهور | النطاق | قرار الفرز الأولي |
|---|---:|---|---|
| `Cannot read properties of undefined (reading 'map')` | 12 أغسطس | `/parent/children` | مراجعة حواجز القيم الاختيارية؛ مرتبط بنشر قديم. |
| `Cannot read properties of undefined (reading 'length')` | 12 أغسطس | `/parent` | مراجعة حواجز القيم الاختيارية؛ مرتبط بنشر قديم. |
| `audit_logs` يتلقى `academy-1` بدل UUID | 12 أغسطس | عدة صفحات | عيب كتابة يجب إصلاحه في طبقة المزامنة/التدقيق. |
| بيانات الأكاديمية غير متاحة | 13 أغسطس | `/settings` | مراجعة معالجة الجلسة غير المرتبطة في الإعدادات. |
| عمود `messages.recipient_name` غير موجود | 10 أغسطس | `/messages` | مراجعة توافق مخطط قاعدة البيانات مع صف الإدراج. |
| UUID فارغ عند إدراج مجموعة | 10 أغسطس | `/onboarding` | مراجعة تطبيع UUID الاختياري قبل الحفظ. |
| أخطاء WhatsApp الاختبارية | 12 أغسطس | `/api/whatsapp/test` | إعداد حساب Meta/رقم الاختبار غير مكتمل؛ لا تُحوّل إلى رسالة حقيقية ولا تُعالج بتجاوز الحماية. |

## ملاحظة

أخطاء الأب/الأبناء تعود إلى عمليات نشر أقدم، بينما أظهر الفحص الحي اللاحق للمسارين `/parent` و`/parent/children` تشغيلًا طبيعيًا. ستُفصل العيوب البرمجية القابلة للإصلاح عن متطلبات تفعيل WhatsApp الخارجية.

## حالة المعالجة

| البند | الحالة | المعالجة |
|---|---|---|
| كتابة سجل التدقيق بمعرّف عرض | مُعالَج | أُضيف حارس في طبقة الكتابة يمنع تمرير معرّف أكاديمية غير UUID إلى جداول الإنتاج. |
| حفظ رسالة مع أعمدة واجهة إضافية | مُعالَج | أصبح الحفظ يرسل أعمدة المخطط فقط، بينما تُشتق أسماء العرض عند التحميل داخل الأكاديمية. |
| إنشاء مجموعة بلا مدرس في الإعداد | مُعالَج | لا ينشئ الإعداد مجموعة قبل دعوة مدرس وقبولها؛ وهو يحافظ على قيد قاعدة البيانات وصحة الحضور والصلاحيات. |
| تعريب رسائل وفواصل الواجهة المتبقية | مُعالَج | عُرّبت أزرار الرسائل والإعدادات ورسالة فشل إرسال الرسالة. |
| أخطاء ولي الأمر التاريخية | تم التحقق | التحميل الحالي يحمي `children` و`summary` بقيم افتراضية، وأظهر الاختبار الحي للمسارين تشغيلًا سليمًا؛ لذلك لم يُضف تغيير تخميني. |
| تفعيل WhatsApp | خارج الشيفرة | الخطأ ناتج عن إعداد Meta/الدفع أو رقم الاختبار، ويظل الإرسال الحقيقي محميًا حتى استكمال بيانات الاعتماد. |

> لم يُطبّق أي ترحيل على قاعدة البيانات في هذه الحزمة؛ يقتصر الإصلاح على منطق التطبيق المتوافق مع المخطط القائم.

## ملاحظة تحقق حي لاحقة

في 13 أغسطس 2026، وبعد نشر الالتزام `9992c5d`، فتح مسار `/settings` بحساب المدير وأظهر استثناءً خادميًا بالمعرّف `Digest: 804079072`. تُعامل هذه الملاحظة كخلل جديد قيد التشخيص، ولا تُعد نتيجة إصلاح مكتملة حتى تُراجع سجلات التشغيل ويُعاد اختبار المسار.

### استثناء `/settings` — Digest `804079072`

**السبب الجذري:** يمكن أن تُنفَّذ مكوّنات App Router المتوازية قبل اكتمال تهيئة لقطة الأكاديمية في التخطيط. كانت صفحة الإعدادات تستدعي `requireRole` ثم تقرأ `MiscService.getAcademy()` بشكل متزامن؛ وفي نافذة السباق هذه تعيد طبقة العزل لقطة فارغة عمدًا، فتطلق خدمة الأكاديمية الاستثناء بدلاً من المخاطرة بقراءة بيانات مستأجر آخر.

**الإصلاح:** استُبدل الاستدعاء في `app/(app)/settings/page.tsx` بـ `await requireScopedRole("ADMIN")` قبل أي قراءة من المخزن. يثبت ذلك جلسة المدير ويحمّل لقطة أكاديميته المقيّدة قبل عرض البيانات، مع الحفاظ على مبدأ الإخفاق الآمن للجلسات غير المرتبطة.

**التحقق المحلي:** اجتاز فحص TypeScript، والبناء الإنتاجي، واختبار Playwright المحدد لفتح الإعدادات بحساب المدير (`1 passed`).
