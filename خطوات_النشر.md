# 🚀 خطوات نشر MY Academy أونلاين (GitHub + Vercel)

> لـ Windows. كل الأوامر في **CMD** أو **PowerShell** في فولدر المشروع.
> الفولدر عندك: `C:\Users\User\Downloads\my-academy (1)`

---

## 🟦 المرحلة 1 — ارفع الكود على GitHub (من CMD)

### 1) تأكد إن Git متطبّ عندك
```
git --version
```
- لو طلع رقم (زي `git version 2.43`) → تمام، كمل.
- لو قال "not recognized" → نزّل Git من **git-scm.com** وثبّته، بعدين اقفل CMD وافتحه تاني.

### 2) افتح CMD في فولدر المشروع
```
cd "C:\Users\User\Downloads\my-academy (1)"
```

### 3) حط الكود في Git (commit)
```
git init
git add .
git commit -m "MY Academy"
```
> لو سألك عن الإيميل/الاسم أول مرة:
> ```
> git config --global user.email "your@email.com"
> git config --global user.name "Your Name"
> ```
> بعدها كرّر `git commit -m "MY Academy"`.

### 4) اعمل Repository على GitHub
- ادخل **github.com** → سجّل/ادخل → دوس **+** فوق يمين → **New repository**.
- الاسم: `my-academy`
- اختار **Private** (مهم: خاص عشان الكود).
- **متظبطش** أي حاجة (لا README، لا .gitignore) → **Create repository**.

### 5) اربط وارفع (push)
GitHub هيديك لينك زي `https://github.com/USERNAME/my-academy.git`. ارجع CMD:
```
git branch -M main
git remote add origin https://github.com/USERNAME/my-academy.git
git push -u origin main
```
- لو سألك على username/password → استخدم **Personal Access Token** (من GitHub → Settings → Developer settings → Tokens) كباسورد.
- أول مرة ممكن تطلب تثبت "Git Credential Manager" → وافِق.

✅ **نتيجة المرحلة 1:** الكود بقى على GitHub. (`.env.local` مابيرفعش — ده صح، المفاتيح سرّية.)

---

## 🟩 المرحلة 2 — انشر على Vercel

### 1) سجّل في Vercel
- ادخل **vercel.com** → **Sign Up** → **Continue with GitHub** (بحساب GitHub بتاعك).

### 2) اعمل مشروع جديد
- بعد ما تدخل → **Add New… → Project**.
- هتلاقي قايمة بـ repos بتاعتك → دوس **Import** جنب `my-academy`.

### 3) حط الـ Environment Variables (مهم جدًا)
قبل ما تدوس Deploy، انزل تحت لـ **Environment Variables** وضيف كل المتغيرات دي (انسخ قيمها من ملف `.env.local` عندك):

| المتغيّر | القيمة |
|------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | من .env.local |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | من .env.local |
| `SUPABASE_SERVICE_ROLE_KEY` | من .env.local |
| `RESEND_API_KEY` | من .env.local |
| `SENTRY_DSN` | من .env.local |
| `NEXT_PUBLIC_SENTRY_DSN` | من .env.local |
| `UPSTASH_REDIS_REST_URL` | من .env.local |
| `UPSTASH_REDIS_REST_TOKEN` | من .env.local |

### 4) Deploy
- دوس **Deploy** → استنى 2-3 دقايق (بيعمل build).
- لما يخلّص → هتلاقي **🎉 Congratulations** + لينك موقعك (زي `my-academy.vercel.app`).

✅ **نتيجة المرحلة 2:** بقى عندك موقع **لايف على الإنترنت**! تقدر تبعته لأي حد.

---

## 🟨 بعد النشر — اختبر اللينك الجديد
1. افتح اللينك (my-academy-xxx.vercel.app).
2. ادخل `admin@myacademy.edu` / `demo1234` → المفروض يفتح.
3. جرّب `/signup` → انضمام طالب بكود `my-academy`.
4. افتح `/api/health` → المفروض `"status":"healthy"`.

---

## ⚠️ ملاحظات مهمة
- **اللينك اللي هتاخده** (vercel.app) مؤقت-ish. لو عايز دومين خاص (زي `myacademy.com`)، ده شغل تاني (شراء دومين + ربطه).
- **الـ env vars** لازم تتحط على Vercel (مش هتشتغل من .env.local على السيرفر).
- **.env.local ميرفعش** على GitHub — ده آمن. بس احرص إن الـ repo **Private**.
- كل ما تعدّل كود وتعمل `git push` → Vercel بيعمل redeploy أوتوماتيك. ✨

---

## لو وقفت في أي خطوة:
- `git push` رفض → غالبًا Token. اعمل Personal Access Token من GitHub واستخدمه.
- Vercel build فشل → ابعتلي رسالة الخطأ من Vercel.
- الموقع فتح بس error → افتح `/api/health` وقوللي اللي بيظهر.

ابدأ المرحلة 1 وقوللي أول ما تخلّص `git push`.
