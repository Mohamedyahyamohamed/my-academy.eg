# دليل تجربة MY Academy — جرّب كل حاجة

> سجّل دخول بـ `admin@myacademy.edu` / `demo1234` — ده المدير، بيقدر يشوف ويعمل كل حاجة.
> كل الصفحات اتأكد إنها شغّالة (200). البيانات موجودة: 8 طلاب، 4 جروبات، 44 دفعة.

---

## 🔥 أول 4 حاجات تجربهم (الأكثر إثارة)

### 1) QR الحضور (أحلى فيتشر)
- روح **Lessons** → افتح أي حصة → دوس **"Start QR Session"** (أو افتح صفحة `/checkin`).
- هيتولّد QR كود موقّع بـ HMAC (صالح ثواني معدودة).
- امسح الكود من جهاز تاني / موبايل → يسجّل حضور.
- جرّب الرجوع **Teacher-scan** (المدرّس يمسح كود الطالب) كمان.

### 2) Dashboard التحليلات
- روح `/dashboard` → شوف الكروت (طلاب، دفعات، حضور)، الجرافات، و**قائمة الطلاب المعرضين للخطر (at-risk)**.

### 3) ملف طالب كامل
- **Students** → افتح أي طالب → شوف: حضوره، درجاته، دفعاته، والـ trends (جراف الحضور والدرجات). من هنا تقدر تعدّله/توقفه.

### 4) غرفة الدردشة ولي الأمر ↔ المدرّس
- **Messages** → شوف المحادثات. (بدّل لوجن لـ `parent@` و`teacher@` عشان تشوف الطرفين.)

---

## 📋 جولة كاملة (Admin) — دلّ على كل قسم

| القسم | الروت | جرّب إيه |
|------|------|---------|
| Dashboard | `/dashboard` | الكروت + الجرافات + at-risk |
| الطلاب | `/students` | افتح ملف طالب → عدّل بيانات / أضف طالب جديد (Add Student) |
| الجروبات | `/groups` | افتح جروب → شوف الأعضاء والمدرّس |
| الحصص | `/lessons` | أنشئ حصة جديدة (New Lesson) |
| الحضور | `/checkin` | QR student-scan و teacher-scan |
| الدفعات | `/payments` | شوف الـ 44 دفعة → علّم واحدة "مدفوعة" (Record Payment) |
| الامتحانات/الدرجات | `/grades` | سجّل درجة لطالب |
| الواجبات | `/homework` | اكتب واجب → افتحه → شوف التسليمات |
| الإشعارات | `/notifications` | شوف/أرسل إشعار |
| الرسائل | `/messages` | محادثات ولي ↔ مدرّس |
| التقارير | `/reports` | ولّد تقرير لطالب/جروب |
| التحليلات | `/reports` (أو analytics) | Insights |
| الإعدادات | `/settings` | بيانات الأكاديمية، المدرّسون، الكورسات، المساعدون |
| الاشتراك/الخطط | `/billing` | شوف خطتك + الاستهلاك (students/teachers/groups) |
| سجل النشاط | Audit logs (من الإعدادات) | كل العمليات المسجّلة |

---

## 👥 جرّب كل دور (بدّل اللوجن)

اطلع (Logout) وادخل بكل دور عشان تشوف البوابة بتاعته:

| الدور | الإيميل | بيشوف إيه |
|------|------|------|
| مدرّس | `teacher@myacademy.edu` | جروباته، طلابه، الحضور، الواجبات، `/teacher/assistants` |
| ولي أمر | `parent@myacademy.edu` | أبنائه: حضور، درجات، دفعات، واجبات، رسائل |
| طالب | `student@myacademy.edu` | دروسه، درجاته، واجباته، تقدّمه (`/student/progress`) |

---

## 🧪 تجارب متقدمة (للتأكد من المنطق)

- **حدود الاستهلاك:** روح `/billing` → شوف حدود الـ Free (30 طالب). جرّب تضيف طلاب لحد ما يوصلوا الحد → المفروض يرفض.
- **عزل الأكاديمية:** ادخل بـ `admin-b@test.com` / `demo1234` (أكاديمية تانية) → المفروض ماتشوفش أي بيانات الأكاديمية الأولى.
- **الحماية:** اطلع من اللوجن، وحاول تدخل `/students` مباشرة → المفروض يرجّعك للوجن (307).

---

## ⚡ لو حصل أي مشكلة وأنت بتجرب
قوللي: **الصفحة اللي فتحتها + اللي دوس عليه + الرسالة/اللي حصل**. وأنا أصلح فورًا (السيرفر شغّال، التعديلات بتيجي سريعة).
